<h3>Here are all of the shoe brands available in our stores</h3>

<% if @brands.any? %>
  <ul>
    <% @brands.each do |brand| %>
      <li><a href="/brands/<%= brand.id %>"><%= brand.name + " " + brand.price.to_s %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no brands available</p>
<% end %>
